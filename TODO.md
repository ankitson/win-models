# TODO

- Replace the installed-package Unsloth Studio patch/rebuild flow with a cleaner integration layer. Prefer a maintained Studio source fork or formal overlay patch set over patching `site-packages` in-place.
- Collapse the current layout CSS fallbacks into one systemic Tailwind fix for arbitrary/custom-property utilities such as `w-(--sidebar-width)` and `max-w-(--thread-content-max-width)`.
- Move the voice/audio UI changes into explicit components and named classes so future Studio rebuilds do not require whack-a-mole shims.
- Keep native-audio and ASR transcript history covered by browser/build tests before removing any defensive fallbacks.
- LM Studio detour notes: its Windows Caddy route already works at `lmstudio.win.ankitson.com`, and `/api/v1/models` exposes richer model metadata than OpenAI `/v1/models` (context length, quantization, loaded instances, vision/tool-use flags). However, the local Unsloth GGUF models do not expose LM Studio's structured `capabilities.reasoning` config, even on LM Studio `0.4.18+1`; `/api/v1/chat` rejects `reasoning` with "does not expose reasoning configuration." The GGUF templates include `enable_thinking`, and llama.cpp can reason on/off, but LM Studio's API layer requires model-index reasoning metadata. Stay on the Unsloth path for now unless we add an adapter or fix LM Studio model metadata.
- Bifrost prefill logging note: assistant prefill/continue requests intentionally send the edited assistant prefix as the last `input_history` assistant message, then Bifrost logs the generated continuation as `output_message` assistant. Its flattened `content_summary` can look like two assistant messages in a row even when Unsloth UI state is correct.
- llama.cpp assistant prefill note: `--prefill-assistant` is enabled by default, so we should not need to send explicit `continue_final_message` flags when the request already ends with one partial assistant message. The important thing to verify is the exact OpenAI message shape reaching llama.cpp: avoid two trailing assistant messages, avoid duplicating the edited assistant prefix, and avoid adding a fresh assistant-generation marker after the prefix.
